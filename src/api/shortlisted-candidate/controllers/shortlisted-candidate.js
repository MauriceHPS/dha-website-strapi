'use strict';

/**
 * shortlisted-candidate controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::shortlisted-candidate.shortlisted-candidate');
