'use strict';

/**
 * shortlisted-candidate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::shortlisted-candidate.shortlisted-candidate');
